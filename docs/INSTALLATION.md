# ระบบวิเคราะห์ผลสอบ O-NET
## คู่มือการติดตั้งบน Hosting

### ✅ ความต้องการของระบบ (System Requirements)

- **PHP**: 7.4 หรือสูงกว่า
- **MySQL**: 5.7 หรือสูงกว่า (หรือ MariaDB 10.2+)
- **PHP Extensions**: PDO, PDO_MySQL
- **Disk Space**: อย่างน้อย 50 MB
- **Memory**: อย่างน้อย 128 MB

---

## 📦 ขั้นตอนการติดตั้ง

### 1. อัปโหลดไฟล์

อัปโหลดไฟล์ทั้งหมดไปยัง hosting ของคุณ (ผ่าน FTP, cPanel File Manager, หรือ SSH)

**ไฟล์ที่ต้องอัปโหลด:**
```
/
├── index.php
├── import.php
├── settings.php
├── install.php
├── db.php
├── functions.php
├── style.css
├── sample_data/
│   ├── students.csv
│   ├── mapping.csv
│   └── scores.csv
└── .htaccess
```

**ไฟล์ที่ไม่ต้องอัปโหลด:**
- `onet.db` (SQLite database - ไม่ใช้บน hosting)
- `onet_demo.db`
- `install_sqlite.php`
- `demo_setup.php`
- `config_test.php`
- `migrate_*.php`

---

### 2. สร้างฐานข้อมูล MySQL

#### ผ่าน cPanel:
1. เข้า **cPanel** → **MySQL Databases**
2. สร้างฐานข้อมูลใหม่ เช่น `onet_db`
3. สร้าง MySQL User และตั้งรหัสผ่าน
4. เพิ่ม User เข้าฐานข้อมูลพร้อม **All Privileges**
5. จดบันทึก:
   - Database Name
   - Username
   - Password
   - Hostname (มักเป็น `localhost`)

#### ผ่าน phpMyAdmin:
1. เข้า **phpMyAdmin**
2. คลิก **New** → ตั้งชื่อฐานข้อมูล
3. เลือก Collation: `utf8mb4_unicode_ci`
4. คลิก **Create**

---

### 3. รันตัวติดตั้ง (Installer)

1. เปิดเบราว์เซอร์ไปที่ `http://yourdomain.com/install.php`
2. กรอกข้อมูลฐานข้อมูล:
   - **Database Host**: `localhost` (หรือตาม hosting ของคุณ)
   - **Database Name**: ชื่อฐานข้อมูลที่สร้างไว้
   - **Database Username**: ชื่อผู้ใช้ MySQL
   - **Database Password**: รหัสผ่าน MySQL
3. คลิก **ติดตั้ง**
4. รอจนกว่าจะแสดงข้อความ "ติดตั้งสำเร็จ!"

**หมายเหตุ:** ตัวติดตั้งจะ:
- สร้างไฟล์ `config.php` อัตโนมัติ
- สร้างตารางฐานข้อมูลทั้งหมด
- ตั้งค่า indexes และ foreign keys

---

### 4. ตั้งค่า File Permissions

ตั้งค่าสิทธิ์ไฟล์ให้ถูกต้อง:

```bash
# ไฟล์ PHP ทั่วไป
chmod 644 *.php

# ไดเรกทอรี
chmod 755 sample_data/

# ไฟล์ที่ต้องเขียนได้ (ถ้ามี)
chmod 666 settings.json
```

**สำคัญ:** ไฟล์ `config.php` ควรมีสิทธิ์ `644` เพื่อความปลอดภัย

---

### 5. นำเข้าข้อมูล

1. ไปที่ `http://yourdomain.com/import.php`
2. นำเข้าข้อมูลตามลำดับ:
   - **📋 นักเรียน** (students.csv)
   - **🎯 ตัวชี้วัดและข้อสอบ** (mapping.csv)
   - **📊 คะแนน** (scores.csv) - เลือกวิชาก่อนนำเข้า

**ทางเลือก:** ใช้ฟีเจอร์ **📋 วางข้อมูล** เพื่อ copy-paste จาก Excel/Google Sheets โดยตรง

---

### 6. ตั้งค่าระบบ (Optional)

ไปที่ `http://yourdomain.com/settings.php` เพื่อปรับแต่ง:

- **Percentile Thresholds**: เกณฑ์การจัดกลุ่มนักเรียน (p80, p60, p40, p20)
- **Weakness Threshold**: เกณฑ์จุดอ่อน (default: 50%)
- **Subject-Specific Settings**: ตั้งค่าแยกตามวิชา

---

## 🔒 การรักษาความปลอดภัย

### 1. ลบไฟล์ติดตั้ง
หลังติดตั้งเสร็จ **ลบหรือเปลี่ยนชื่อ** `install.php`:
```bash
rm install.php
# หรือ
mv install.php install.php.bak
```

### 2. ตรวจสอบ .htaccess
ไฟล์ `.htaccess` ช่วยป้องกัน:
- การเข้าถึง `config.php` โดยตรง
- Directory listing
- SQL injection (บางส่วน)

### 3. สำรองข้อมูล
สำรองฐานข้อมูลเป็นประจำผ่าน:
- cPanel → **Backup**
- phpMyAdmin → **Export**
- Command line: `mysqldump`

---

## 🐛 การแก้ปัญหา (Troubleshooting)

### ปัญหา: "Connection failed" เมื่อติดตั้ง

**สาเหตุ:**
- ข้อมูล MySQL ไม่ถูกต้อง
- MySQL service ไม่ทำงาน
- Firewall บล็อก connection

**วิธีแก้:**
1. ตรวจสอบ username/password ใน cPanel
2. ลอง hostname อื่น: `127.0.0.1` แทน `localhost`
3. ติดต่อ hosting support

---

### ปัญหา: "Permission denied" เมื่อสร้าง config.php

**วิธีแก้:**
1. สร้างไฟล์ `config.php` เองด้วย File Manager
2. คัดลอกเนื้อหาจาก `config.sample.php`
3. แก้ไขข้อมูล MySQL
4. บันทึกไฟล์

---

### ปัญหา: หน้าเว็บแสดง "500 Internal Server Error"

**วิธีแก้:**
1. ตรวจสอบ PHP error log
2. เช็ค file permissions (ควรเป็น 644 สำหรับ .php)
3. ตรวจสอบว่า PHP extensions ครบ:
   ```bash
   php -m | grep -E 'pdo|mysql'
   ```

---

### ปัญหา: ไม่สามารถนำเข้าข้อมูลได้

**วิธีแก้:**
1. ตรวจสอบ `upload_max_filesize` และ `post_max_size` ใน php.ini
2. เพิ่มค่าใน `.htaccess`:
   ```apache
   php_value upload_max_filesize 10M
   php_value post_max_size 10M
   ```
3. ใช้ฟีเจอร์ **วางข้อมูล** แทนการอัปโหลดไฟล์

---

## 📞 การสนับสนุน

หากพบปัญหาในการติดตั้ง:
1. ตรวจสอบ error log ของ hosting
2. ลองติดตั้งบน local environment ก่อน
3. ตรวจสอบว่าข้อมูล CSV ถูกต้อง (UTF-8 encoding)

---

## 🎓 การใช้งาน

หลังติดตั้งเสร็จ:

1. **หน้าหลัก** (`index.php`):
   - เลือกชั้น/ห้อง/วิชา
   - ดูภาพรวม (Overview)
   - ดูรายบุคคล (Individual Report)

2. **นำเข้าข้อมูล** (`import.php`):
   - อัปโหลด CSV หรือ copy-paste
   - นำเข้าทีละวิชา

3. **ตั้งค่า** (`settings.php`):
   - ปรับเกณฑ์การจัดกลุ่ม
   - ตั้งค่าแยกตามวิชา

---

## 📋 Checklist การติดตั้ง

- [ ] อัปโหลดไฟล์ทั้งหมด
- [ ] สร้างฐานข้อมูล MySQL
- [ ] รัน install.php
- [ ] ตั้งค่า file permissions
- [ ] นำเข้าข้อมูลนักเรียน
- [ ] นำเข้าข้อมูลตัวชี้วัด
- [ ] นำเข้าข้อมูลคะแนน
- [ ] ลบ install.php
- [ ] ทดสอบระบบ
- [ ] ตั้งค่า backup อัตโนมัติ

**ติดตั้งเสร็จแล้ว!** 🎉
